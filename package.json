{
  "name": "agente-reunioes",
  "version": "1.0.1",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "workspaces": [
    "packages/contracts",
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:backend": "npm run dev -w @meeting-bot/backend",
    "dev:frontend": "npm run dev -w @meeting-bot/frontend",
    "teams:login": "node scripts/teams-login.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
