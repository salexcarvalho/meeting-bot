{
  "name": "agente-reunioes",
  "version": "0.3.0",
  "private": true,
  "workspaces": [
    "packages/contracts",
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:backend": "npm run dev -w @meeting-bot/backend",
    "dev:frontend": "npm run dev -w @meeting-bot/frontend"
  },
  "engines": {
    "node": ">=24"
  }
}
